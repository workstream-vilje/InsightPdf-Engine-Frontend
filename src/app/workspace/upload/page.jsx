// ProjectCanvas is rendered by the parent workspace layout (layout.jsx).
// This page file exists only to satisfy Next.js routing for /workspace/upload.
export default function WorkspaceUploadPage() {
  return null;
}
